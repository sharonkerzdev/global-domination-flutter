# DO NOT EDIT -- overwritten on every update.
#
# Workflow customization surface for gds-code-review. Mirrors the
# agent customization shape under the [workflow] namespace.

[workflow]

# --- Configurable below. Overrides merge per BMad structural rules: ---
#   scalars: override wins • arrays (persistent_facts, activation_steps_*): append
#   arrays-of-tables with `code`/`id`: replace matching items, append new ones.

# Steps to run before the standard activation (config load, greet).
# Overrides append. Use for pre-flight loads, compliance checks, etc.

activation_steps_prepend = []

# Steps to run after greet but before the workflow begins.
# Overrides append. Use for context-heavy setup that should happen
# once the user has been acknowledged.

activation_steps_append = []

# Persistent facts the workflow keeps in mind for the whole run
# (standards, compliance constraints, stylistic guardrails).
# Distinct from the runtime memory sidecar — these are static context
# loaded on activation. Overrides append.
#
# Each entry is either:
#   - a literal sentence, e.g. "Code review must prioritize risk and correctness over stylistic preference."
#   - a file reference prefixed with `file:`, e.g. "file:{project-root}/docs/standards.md"
#     (glob patterns are supported; the file's contents are loaded and treated as facts).

persistent_facts = [
  "file:{project-root}/**/project-context.md",
]

# When true, the workflow does not HALT for human checkpoints after activation:
# auto-proceed from Step 1 summary, skip live resolution prompts in Step 4,
# automatically apply all patch findings (same as "Apply every patch"), and
# end without the Step 7 "what next" menu. decision_needed items are written
# to the story file unchecked and must be resolved outside this run.
# Override wins.

continuous_to_completion = false

# Scalar: executed when the workflow reaches Step 4 (Present and Act),
# after the final outputs are produced. Override wins.
# Leave empty for no custom post-completion behavior.

on_complete = ""
